{
  "name": "heap-it",
  "version": "1.0.0",
  "description": "a test for binary heaps",
  "main": "heap.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src -d lib"
  },
  "author": "",
  "license": "ISC"
}
